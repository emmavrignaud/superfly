# System binary (not pip-installable — must be on PATH before running the pipeline):
#   ffmpeg  — H.264/.m4v camera files, *-converted.mp4 prep, IPython Video in notebooks
#   Install: https://ffmpeg.org/download.html
#            Windows: winget install Gyan.FFmpeg  (or choco install ffmpeg)

# Core computer vision / tracking
opencv-python>=4.8
scikit-image>=0.21      # watershed bbox splitting (src/watershed_split.py; enabled in config.yaml)

numpy>=1.24
supervision>=0.18
inference-sdk>=0.13   # lightweight HTTP client; full `inference` server not needed
filterpy>=1.4         # Kalman filter primitives used by src/kalmanfilter.py + src/ocsort.py
lap>=0.5.12             # fast Jonker-Volgenant matching in OC-SORT (scipy fallback if missing)

# Data / ML
pandas>=2.0
scipy>=1.11
scikit-learn>=1.3
scikit-posthocs>=0.9  # Dunn/Holm post-hoc tests (src/classification.py)
umap-learn>=0.5       # embedding dim-reduction (src/latent_space.py, src/statistics.py)

# Visualisation
plotly>=5.18         # interactive diagnostics + analysis figures
matplotlib>=3.7      # static plot fallbacks
PyQt5>=5.15          # preprocessing / ROI GUIs
PySide6>=6.6         # labeler GUI (QML)
colorcet>=3.0        # Glasbey palette for track IDs
kaleido>=0.2         # plotly static image export

# Config
pyyaml>=6.0
ruamel.yaml>=0.18    # comment-preserving YAML writes (scripts/app.py edits config.yaml in place)

# Notebook support (optional, only needed for .ipynb)
ipython>=8.0
jupyterlab>=4.0

# Tests (README: pytest tests/ -v)
pytest>=7.0

# Vendored HOTA evaluator (run_tracking metrics / parameter_tuning)
-e external/TrackEval
